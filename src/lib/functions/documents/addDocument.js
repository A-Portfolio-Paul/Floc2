import {newId} from 'newid'




export const addDocument = () =>{
    console.log('addDoc running.......')
    const newDoc = {
        export const document1 = {
            docId:newId(),
            createDate:,
            createdBy:'ur-101',
            title:'Causes & Treatments for Pancreatitus'
        }
    }
}