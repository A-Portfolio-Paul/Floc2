import { documentMock } from './main';
import util from  'util'


describe('Doc Moc', () => {
	it('', () => {
		
    // console.log(util.inspect(test))
    console.dir(documentMock, { depth: null })
		expect('').toStrictEqual('');
	});
});
