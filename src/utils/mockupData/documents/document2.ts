import { mockIds } from '../mockids'

 //this works first data srtruc in ts
export const document2:{ docId: string, createDate: string, createdBy: string, title: string } =
    { "docId": mockIds.doc2, "createDate": "2022-03-25", "createdBy": mockIds.secondUserId, "title":"Which Bicycle should I buy"}

    